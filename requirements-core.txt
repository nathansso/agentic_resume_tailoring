# Core dependencies — no heavyweight optional extras.
# Use this for Docker base images or lightweight environments.
# For full functionality (LinkedIn web scraping + semantic skill matching +
# layout-aware docling document parsing):
#   pip install -r requirements-full.txt
# Lightweight PDF text extraction (docling fallback)
pypdf
openai
python-dotenv
sqlmodel
langchain
langchain-openai
langgraph
requests
python-docx
pydantic
# For resume-matcher logic
nltk
scikit-learn
# Optional but good for graph
networkx
# LinkedIn scraping (PDF/HTML path only — browser not required)
beautifulsoup4
# Web API
fastapi
uvicorn[standard]
python-multipart
python-jose[cryptography]
itsdangerous
# PDF export — LaTeX/pdflatex (installed via apt in Dockerfile; no Python deps needed)
# PostgreSQL driver (used when DATABASE_URL points to Supabase or other hosted Postgres)
psycopg2-binary
# Supabase client — Auth + Realtime (Phase 2 cloud features)
supabase
