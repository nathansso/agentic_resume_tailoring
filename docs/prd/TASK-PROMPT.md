# Task Prompts for Claude Code

Copy the relevant prompt and paste it into a fresh Claude Code session.
Run one task at a time. Task 10 requires Task 08 to be complete first.
Task 09 can be run at any time independently.

---

## Task 07 — Profile Page Enhancements

```
Read docs/prd/07-profile-page-enhancements.md in full before writing any code.

Then:
1. Read every file listed in the "Required reading" section of that file.
2. Run `python run_tests.py` and confirm the baseline passes before touching anything.
3. Implement everything described in the task, in the order the sections are written.
4. Run `python run_tests.py` again when done. All tests must pass.
5. Do not implement anything outside the scope of the task file.

Do not start writing code until you have finished all required reading.
```

---

## Task 08 — Job Page Enhancements

```
Read docs/prd/08-job-page-enhancements.md in full before writing any code.

Then:
1. Read every file listed in the "Required reading" section of that file.
2. Run `python run_tests.py` and confirm the baseline passes before touching anything.
3. Implement everything described in the task, in the order the sections are written.
4. Run `python run_tests.py` again when done. All tests must pass.
5. Do not implement anything outside the scope of the task file.

Do not start writing code until you have finished all required reading.
```

---

## Task 09 — Data Tab Cleanup

```
Read docs/prd/09-data-tab-cleanup.md in full before writing any code.

Then:
1. Read every file listed in the "Required reading" section of that file.
2. Run `python run_tests.py` and confirm the baseline passes before touching anything.
3. Implement everything described in the task, in the order the sections are written.
4. Run `python run_tests.py` again when done. All tests must pass.
5. Do not implement anything outside the scope of the task file.

Do not start writing code until you have finished all required reading.
```

---

## Task 10 — Persistent Chat Memory

```
Read docs/prd/10-persistent-chat-memory.md in full before writing any code.

Note: Task 08 must be complete before starting this task.

Then:
1. Read every file listed in the "Required reading" section of that file.
2. Run `python run_tests.py` and confirm the baseline passes before touching anything.
3. Implement everything described in the task, in the order the sections are written.
4. Run `python run_tests.py` again when done. All tests must pass.
5. Do not implement anything outside the scope of the task file.

Do not start writing code until you have finished all required reading.
```
